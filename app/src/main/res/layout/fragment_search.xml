<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <import type="android.view.View"/>
        <variable
            name="searchWord"
            type="java.lang.String" />
        <variable
            name="searchInteractor"
            type="com.whycody.wordslife.search.SearchInteractor" />
        <variable
            name="resultsAvailable"
            type="boolean" />
    </data>

    <androidx.core.widget.NestedScrollView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?attr/colorSecondary"
        tools:context=".search.SearchFragment">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:paddingTop="40dp">

            <fragment
                android:id="@+id/currentLanguageFragment"
                android:name="com.whycody.wordslife.current.language.CurrentLanguageFragment"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" />

            <EditText
                android:id="@+id/searchWordInput"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/search"
                android:imeOptions="actionSearch"
                android:background="@drawable/s_search_button"
                android:padding="14dp"
                android:inputType="text"
                android:paddingStart="25dp"
                android:paddingEnd="25dp"
                android:textSize="16sp"
                android:maxLines="1"
                android:text="@{searchWord}"
                android:layout_marginTop="25dp"
                android:layout_marginStart="@dimen/app_padding"
                android:layout_marginEnd="@dimen/app_padding" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="32dp"
                android:visibility="@{resultsAvailable ? View.VISIBLE : View.GONE}"
                android:paddingStart="@dimen/app_padding"
                android:paddingEnd="@dimen/app_padding"
                android:paddingBottom="8dp">

                <ImageView
                    android:layout_width="8dp"
                    android:layout_height="8dp"
                    android:layout_gravity="center"
                    android:src="@drawable/circle"/>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textStyle="bold"
                    android:layout_marginStart="10dp"
                    android:textColor="?attr/colorPrimaryVariant"
                    android:textSize="16sp"
                    android:text="@{@string/word_search_results(searchWord)}"/>

            </LinearLayout>

            <TextView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="@{resultsAvailable ? View.GONE : View.VISIBLE}"
                android:padding="@dimen/app_padding"
                android:layout_marginTop="50dp"
                android:textSize="15sp"
                android:gravity="center"
                android:text="@{@string/no_word_search_results(searchWord)}"/>

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/searchResultRecycler"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:visibility="@{resultsAvailable ? View.VISIBLE : View.GONE}"
                android:overScrollMode="never"
                android:layoutAnimation="@anim/layout_fall_down"/>

            <TextView
                android:id="@+id/showMoreResults"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/show_more_results"
                android:padding="@dimen/app_padding"
                android:onClick="@{()->searchInteractor.showMoreResults()}"
                android:visibility="invisible"
                android:textColor="?attr/colorPrimary"
                android:gravity="center"/>

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

</layout>